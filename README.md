# EVTX Parser

Browser-based parser for Windows Event Log (`.evtx`) files.

## Implementation

The parser implementation is in [`evtx-parser-react/`](./evtx-parser-react/). See that directory for full documentation, architecture details, and usage instructions.
